# Optional: FRED API key for faster data fetching via fredapi library.
# Without this, the app uses the FRED public CSV endpoint.
# Free key: https://fred.stlouisfed.org/docs/api/api_key.html
FRED_API_KEY=your_fred_api_key_here
